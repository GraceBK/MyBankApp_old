<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/splashscreenActivity">

    <activity
        android:id="@+id/splashscreenActivity"
        android:name="fr.mbds.bankapp.SplashscreenActivity"
        android:label="activity_splashscreen"
        tools:layout="@layout/activity_splashscreen" />
    <activity
        android:id="@+id/mainActivity"
        android:name="fr.mbds.bankapp.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/homeFragment"
        android:name="fr.mbds.bankapp.fragment.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/payFragment"
        android:name="fr.mbds.bankapp.fragment.PayFragment"
        android:label="fragment_pay"
        tools:layout="@layout/fragment_pay" />
    <activity
        android:id="@+id/loginActivity"
        android:name="fr.mbds.bankapp.LoginActivity"
        android:label="activity_login"
        tools:layout="@layout/activity_login" />
</navigation>